# Regression log

The regression suite is `gates/gates.sh` (one script, run on every build: mountcheck first, then every
`gates/regress/*.js`). It lives in the repo since #373 - the previous suite (138 PASS lines at #372) existed only in
a sandbox that is gone. The charter's number is the count of PASS lines in `gates/logs/<N>-all.log`; it may only
rise. A gate that is red blocks the build (GATES RED); a build is "gated" only when the log ends GATES GREEN.

## Gates

| gate | what it asserts | ported from |
|---|---|---|
| mountcheck.js | the bundle mounts at 375x679 and 390x844, stamp in the DOM, four tiles, each tab opens with zero app errors | HANDOFF gate 3 |
| 10-gameover.js | card k10: board width and top unchanged after checkmate, no scroll, row Review · Rematch, no Resign | gameover371 |
| 11-lesson.js | cards 3/4: lesson board 375@92 at the demo end and in practice, note box 75px, unchanged after one practice move, no scroll | lesson371 |
| 12-hint.js | card 5 at 375/390/320: board unmoved by the hint, hint text unclipped in the header, header above the board | gallery372 / hintcap372 |
| 13-play-after-moves.js | card k8 and a real Pass & Play game: one width and one top across five plies, 351 on Kunal's phone, no scroll | jump370 |
| 14-uat-review-card.js | the Review UAT card: seven captioned checkpoints and the state each claims | new (#373) |
| 20-review.js | TC-R01..R09, R11..R14: import < 90 s, summary content and pinned footer, Skills jump, board 349 with one top/width across plies, bars one height, ratings from headers, 1-0 at mate, rank-8 badge inside the board, verdict chips, analysis board, sheet and navigation, cached re-import < 8 s with identical numbers, eval graph, stored accounts survive a reload | rev342 / mate371 / summary368 / graph369 / acct353 (partly) |
| 21-review-brilliant.js | TC-R10: the brilliancy's reason names the sacrifice, the forcing line and the comparison; the play-out moves a piece; only the one allowed engine trap | bril357gate / why354 |
| 30-p1-fixes.js | the #373 P1 fixes: Home ☰, menu sheet to the bottom, Pass & Play setup fits, Next puzzle advances, 40px targets, Moves toggle leaves the board still | new (#373) |
| 15-gallery-playall.js | the recording-free gallery gate Kunal's feedback session asked for: drives "Play all" at 375x730 and 375x812, every caption reached in order, no page scroll, no board shrink inside a card, the RECORDING COMPLETE frame | new (#376) |
| 32-plylog.js | the ply log is dev-gated and temporary: plies stepped with the switch off leave nothing behind when the readout is later turned on, plies stepped with it on are recorded and printed, switching off clears the buffer | new (#376) |
| 33-reproducible-review.js | the stuck-worker timeout read straight out of the bundle under test (at least 20 s), plus reproducibility: two fresh contexts with the eval cache cleared, same PGN, sequential; accuracy within 1.0 point, all nine verdict counts identical, run times within 12 s, and 10.Nxb5 not scored as a mistake | rebuilt from repro373, which died with its sandbox (#376) |
| 31-antagonist373.js | the antagonist's three numbers on #373 (label inside the board on the h-file, 0-1 for a mate by Black on the analysis board, Analyze/Copy hit 6px above and below the chip) and the Online lobby's Back | new (#374) |

## Counts

| build | PASS lines | gates | log |
|---|---|---|---|
| #372 | 138 | the old sandbox's gates.sh (lost with the sandbox) | gatelogs/372-all.log (not in the repo) |
| #373 | 203 (the run of record on the pushed bundle, 22:41 ET; a trial run earlier had 200 before the engine-on label check was added) | 10 | claude/agents/gatelogs/373-all.log |
| #374 | 217 (adds 31-antagonist373.js) | 11 | claude/agents/gatelogs/374-all.log |
| #375 | 160 over 24 gates, in the retiring chat's own suite, which is not in this repo | 24 (theirs) | not in the repo; see flag handover-from-brilliant-chat |
| #376 | 256 across 12 suites (adds 15-gallery-playall 12, 32-plylog 16, 33-reproducible-review 11) | 12 | claude/agents/gatelogs/376-all.log |
| #377 | 258 across 12 suites (33-reproducible-review grows from 11 to 20: four bundle assertions for the single-worker path and a third review at ct_pool=1 that must match the pool's verdicts exactly) | 12 | claude/agents/gatelogs/377-all.log |
| #378 | 272 across 14 suites (adds 34-takeback, 14 assertions; A-11 and the effective-tap-box proof MOVED into the More sheet with the controls rather than being deleted) | 13 | claude/agents/gatelogs/378-all.log |
| #379 | 286 across 14 suites (adds 36-evalbar, 14 assertions. 35-width-containment was written this run and is HELD in gates/pending/ with its reason, not in the suite: it is red on a confirmed defect that is blocked on a decision, and putting it in regress/ would leave main permanently red) | 14 | claude/agents/gatelogs/379-all.log |
| #380 | 379 across 17 suites (adds 37-strip-sync 41 and 38-rev-sheet-reach 45, both from the overnight test lane; 20-review grows 60 -> 73 with no change to the app) | 16 | claude/agents/gatelogs/380-all.log |
| #381 | 430 across 18 suites (adds 39-pz-streak 45; 20-review grows 73 -> 79 as TC-R11's single position assertion becomes four that MOVED WITH the button rather than being deleted) | 17 | claude/agents/gatelogs/381-all.log |
| #381, re-gated | 448 across 19 suites on the SAME bundle (adds 40-reachability 12; 20-review's blind First-move click replaced by a real assertion, TC-RL-033). No build number: gates and docs only, app.js unchanged at md5 9de857145278 | 17 | claude/agents/gatelogs/381d-all.log (381b and 381c are earlier runs of the same bundle, before 10-gameover's width was pinned) |

Not yet ported (the old suite's other harnesses, to be rebuilt as the screens get their charter pass): fit.js (14
phone viewports per board screen), shift346 (26 samples through the engine's think), veteran360 (stored-profile
sweep), skills368 (pure gameSkills), elo349, gist350 / read350, engcrash / trap356, look367, overlay366, open370,
pzafter370, acct353's full survival set, chrome347, kunal364c/d.

## Negative controls: does a gate still fail when the thing it guards is broken?

BUILD-CONTEXT §2 asks for this, and it changed a gate on the day it was first run.

| gate | broken build used | result | what it changed |
|---|---|---|---|
| 33-reproducible-review | the stuck-worker guard put back from 20 s to 4 s, bundled and served | **PASSED, 8 of 8** | The behavioural half cannot see that regression on this machine: a whole 33-ply review finishes here in 9 to 10 s with three workers, so no search approaches even the broken 4 s ceiling. The bug bit on a slower device. The constant is now read directly out of the bundle under test, and the same broken build then goes RED on it. The reproducibility assertions stay, but they are no longer described as protecting the timeout, because they measurably do not. |
| 33-reproducible-review, #377 | the single-worker path put back the way #376 shipped it: `go movetime` with a flat 4 s guard, no depth argument, bundled and served | **FAILED, 4 of 20** - as it should | Three bundle assertions went red (no depth-mode guard; the depth argument not wired to `go depth`; the review not asking for depth 16) AND, unlike the timeout control, THE BEHAVIOURAL HALF FIRED TOO: the ct_pool=1 review published different verdicts from the pool review of the same game in the same build. That is the user-visible harm, measured rather than argued, and it is why this control is worth more than the first one - the fault is reachable on this hardware, so the gate does not have to rely on reading a constant. |
| 33-reproducible-review, #377, UNPLANNED | none - this one was not a control. The gate was run against its own author's FIX | **FAILED, 2 of 20** | The most valuable red of the run, and the only time this suite has caught something its writer did not already know. Putting both review paths on depth 16 did not make them agree: the single-worker run still returned Best 7,6 against the pool's 5,5 and Black 58.8 against 57.7. The difference was the transposition TABLE, not the search - it was cleared at each worker's block start, and block boundaries move with the worker count, so a position's depth-16 score was a function of the DEVICE. The temptation was to weaken the assertion to match the code. It was the assertion that was right. Fixed in the app instead: a table-reset cadence tied to the position index, blocks aligned to it, and the two configurations now agree exactly. |
| 34-takeback, #378 | the `opponent==='computer'` guard removed, so the row appears in Pass & Play too | **FAILED, 1 of 14** - on exactly the right line | "in PASS & PLAY there is no Takeback row at all" went red and nothing else did. The absence half is the half that is easy to get wrong here, because the More sheet opens on that screen too, so a gate that only checked vs Computer would have gone green on a build that gave Pass & Play a takeback Kunal explicitly scoped out. |
| 34-takeback, #378, ITS OWN FIRST RUN | none - the gate was measuring wrongly | **3 red on a CORRECT build** | Worth recording because the reflex is to suspect the app. The MOVES panel renders one token per line ("MOVES\n1.\ne4\ne5"), so counting matches of "<n>. <san>" counted ROWS, and a row holds two plies: a working takeback read 2 -> 1 instead of 2 -> 0. The second cut then counted the empty-state line ("Your moves appear here as you play.") as a move and read 2 -> 1 again. Both were found by DUMPING THE REAL TEXT from a running build instead of reasoning about it, which is the same rule as measure-do-not-read applied to the harness itself. |
| 36-evalbar, #379 | the bar put back to 10px wide with the old #2b2932 track | **FAILED, 6 of 14** | Fires on exactly the six things that changed and nothing else: the Play bar's width at move 0 and after four plies, the track not being the old colour, the track being the measured one, the board being 353, and the Review bar's track. |
| 36-evalbar, #379, THE COLOUR ITSELF | not a gate control - the CHOICE was checked against reality before it shipped | **the proposed colour failed** | Worth recording because it is the same failure mode as a gate that never runs the broken build. The plan computed #6b6978 at over 3:1 from its hex value; screenshotted from a running build it paints rgb(84,82,94) for 2.36:1, BELOW the 3:1 floor that motivated the whole change. Six candidates were built and sampled to find one that clears it. The method was validated against a known answer first: the old #2b2932 sampled 1.12:1, independently reproducing the 1.15:1 Kunal's own decision cites. Measure, do not read, applies to colour too. |
| 37-strip-sync, #380 | none needed - the **#379 bundle itself** is the build immediately before the fix, so the control is the real previous release rather than a synthetic break | **FAILED, 3 of 41** | Fires on exactly the ⏮ assertion and nothing else, at all three runs: scrollLeft 537 of 537 at 375x730 compact, 486 of 486 at 375x730 classic, 622 of 622 at 320x568. Recorded honestly alongside it: the step-forward-then-back path reads 0 on the broken build TOO, because stepping to ply 1 makes chip 1 current and its own ref scrolls the strip to the left edge. That assertion is kept as a guard that the fix did not break the path that already worked, not as evidence of the defect. A gate whose every assertion goes red on the control is usually a gate that is asserting one thing five times. |
| 37-strip-sync, #380, ITS OWN FIRST RUN | none - the gate was claiming a branch it had never entered | **a false GREEN on a branch that never ran** | The run seeded `ct_revCompact=0` to exercise the classic review screen and asserted against it. It was measuring COMPACT the whole time: a `#339` one-time migration overwrites the stored preference on first load unless `ct_revmig339` is already '1', so the seed was silently discarded and the two runs were the same run twice. Caught by the numbers being identical (clientWidth 305, maxScroll 537) where a different branch must produce different ones. Fixed by seeding both keys AND asserting the branch from the painted screen - chevrons present in compact, absent in classic, now 305/537 against 356/486. This is the same family as #375's gate that forced a 3-worker pool and exercised the fixed path twice: **a seed is an intention, and only a measurement of the screen is a branch.** |
| 38-rev-sheet-reach, #380 | one line: `maxHeight:'82vh',overflowY:'auto'` deleted from the `rev-sheet` panel, bundled and served | **FAILED, 14 of 45** | Run twice, on two trees, with identical results. Note WHICH half does the work at which size: at 320x568 the behavioural assertions fire - a row ('1 inaccuracies ›') sits at top -77, off the top of the screen with no way to reach it - but at 375x730, 390x844 and 430x932 the content happens to fit and ONLY the structural assertions (overflowY, maxHeight) go red. A gate written to catch this through the symptom alone would have passed on three of five phones while shipping the bug to all five. That is the #377 timeout lesson again: assert the thing itself in the bundle under test, not only a symptom a roomy device cannot produce. |
| 38-rev-sheet-reach, #380, THE DEFECT ITSELF | not a gate control - the REPORTED defect was checked against reality before anything was fixed | **the report was wrong** | The test lane raised the ⋯ sheet as "below the fold at five of six geometries", with the last row at 847/833/842/848/863/909 against viewports of 568/679/730/761/844/932. Those are the row's position BEFORE the sheet is scrolled. The sheet is maxHeight 82vh with overflowY auto; scrolled to its end the last row sits at 545 (vh 568), 656 (679), 707 (730), 821 (844) and 909 (932, needing no scroll at all). Every row was then checked one at a time and every one is reachable AND tappable. Same class as gate 35's first version climbing past an `overflow:hidden` ancestor, and the same rule CLAUDE.md states as a crop is a reading, not a measurement. **The fix was to invert the gate's claim, not to change the app.** Nothing shipped for this item and that is the correct outcome. |
| 20-review TC-R11, #381 | the Analyze button put back where it was, absolutely positioned on the board's bottom-right corner as #380 shipped it | **FAILED, 8 of 79** | Fires on exactly the four position assertions at both geometries and nothing else. Worth reading the PASSES too: 'analysis board offers Undo and Exit analysis', the two-move test and Undo all still pass on the broken build, because putting the button back on the board does not change what it DOES. That is the control working as intended - the assertion that moved with the button is the only thing standing between A-08 and a silent regression, and the functional half would never have noticed. |
| 39-pz-streak, #381 | the pz-streak fill deleted, so the reserved verdict box goes back to being empty while a hint is up | **FAILED, 21 of 45** | Red at all three geometries on both data branches. Recorded because of what stays GREEN: 'the board does not move when the hint appears' passes on the broken build too - of course it does, an empty box is exactly as tall as a filled one. The board-movement assertions are not evidence of this feature, they are the guard that the feature did not cost anything, and the two are worth keeping apart. |
| 20-review TC-RL-033, #381 re-gate | Start review no longer resetting the ply - the pre-#373 behaviour, restored in one edit | **the OLD gate: 79 pass, 0 FAIL. The NEW gate: 79 pass, 2 fail.** | Both run against the SAME broken bundle, which is the only way to state a gate-blindness finding as a measurement rather than an argument. The old line tapped Start review and then clicked First move, with a comment asserting that Start review resumes at the last viewed ply. True before #373, false since #375 made the button do what it says - so the click was landing on a review already at ply 0 and doing nothing, and it would have hidden the regression completely. The click is gone and the behaviour is asserted. |
| 40-reachability, #381 re-gate, ITS OWN FIRST VERSION | the app's scroller disabled (#root given overflow-y hidden) | **PASSED, 12 of 12** | A gate written specifically to warn about a reachability measurement error, making both halves of that error. (a) It used `scrollIntoView`, which scrolls an `overflow:hidden` box that a finger cannot - the identical reflex that made gate 35's first version climb past an `overflow:hidden` ancestor. (b) It accepted ANY scrollable ancestor rather than one that actually moves the element, so an unrelated scroller elsewhere certified content it does not contain. Rewritten to scroll only USER-scrollable ancestors and re-read the rect, it goes red with scrollers:0 and reached:false. |
| 40-reachability, #381 re-gate, THE FIRST CONTROL | `overflowY:'hidden'` on a `.scroll` div inside the screen | **worthless - every measured number came back byte-identical** | Recorded because the tempting reading is "the gate held". It proves nothing either way: that div is not the scroller serving that screen. The real one is `#root`, styled in index.html, whose own comment states the design that caused two false P0s - "The app scrolls inside here if its content ever overflows, the page never does." A CONTROL THAT DOES NOT MOVE THE MEASUREMENT IS NOT A CONTROL, and the check for that is cheap: if the numbers are identical, the break did not reach the code under test. |
| 10-gameover, #381 re-gate | the MOVES header row's `minHeight:23` removed - the #378 regression exactly, where folding the chips out let that row shrink and the board grew | **PASSED, 12 of 12 - AND IT WAS BLIND** | The board read **367 instead of 351** on the broken build and the gate said nothing, because its only width assertion was "unchanged between mid-game and game over". That is true of a board that is the wrong size the whole way through: a value compared only against itself cannot reveal a change that arrived before the first sample. The #378 close-out fixed this defect in the app and left the gate unable to see it come back. FIXED HERE by pinning the width (351 at 375x679, 390 at 390x844); the same broken bundle now goes 13 pass / 1 fail, and the real bundle 14 / 0. |
| 32-plylog, #381 re-gate, FIRST ATTEMPT | the ply-log READOUT changed to print the buffer regardless of the switch | **worthless - output byte-identical** | Second non-control of the night, same lesson as the one before it. The switch gates the RECORDING as well as the readout, so with recording still gated the buffer is empty whether or not the readout would print it. The break has to reach the thing the assertion is about. |
| 32-plylog, #381 re-gate | the RECORDING ungated: `if(plyLogOnRef.current)` at the push site changed to `if(true)` | **FAILED, 4 of 16** | On exactly the OFF-case assertions - "plies stepped with the switch OFF left nothing in the buffer" and its repeat after the switch is turned off again - which is this gate's central claim and the one it says it proves without a back door. The other twelve, which are about the switch being dev-gated and the readout rendering, correctly stay green. |
| 30-p1-fixes, #381 re-gate | the Home hamburger taken from 46x46 to 34x30, under the 40px tap floor | **FAILED, 2 of 33** | A-04 at both geometries, and nothing else. A narrow control for a broad gate: 30-p1-fixes carries six separate findings, and this one exercises only A-04/A-11. The other five (A-16 sheet bottom, A-13 setup sheet, A-10 puzzle navigation, A-12 Moves toggle) are still without a control of their own. Recorded rather than glossed - "the gate went red" is not the same as "the gate is proved". |
| 12-hint, #381 re-gate | the header-row takeover removed, so a hint no longer replaces the header on phones | **FAILED, 6 of 21** | Three assertions at each of two geometries: the hint header is absent, its clipping check has nothing to read, and it is not above the board. This is the gate's whole subject (A-06/Z-01, "a puzzle hint takes the header row and never moves the board"), so the half that fires is the right half. |
| 13-play-after-moves, #380 | none planned - this one happened by accident and is recorded because it is a real control | **FAILED, 1 of 12, on a bundle nobody meant to gate** | The first #380 suite was built from a tree sitting on the fb-chrome branch, so the bundle carried a change held off main for costing short phones 42-56px of board. This gate read the Pass & Play board at **295 instead of 351 at 375x679**, with the board top 53px lower, and stopped the run in seconds. It is the strongest evidence in this file for keeping 375x679 as a shorter-phone column after Kunal's real phone turned out to be 730: the change was invisible at 730 and obvious at 679. |
| mountcheck, #381 re-gate (the suite audit's 14th control) | the **#378 bundle** (985d61643b88) gated while claiming `#379` | **FAILED, 2 of 16, in 21 seconds** | Exactly as the audit predicted: `stamp is #379` goes red at both geometries before any behavioural gate runs. WITH ONE FINDING ATTACHED: with `CT_EXPECT` UNSET, the same six-build-old bundle passes 16 of 16 and exits 0. `gates.sh` always exports `CT_EXPECT` (defaulting to its own argument) so the suite is immune, but a bare `node gates/mountcheck.js` is not - the same shape as the `gates/.pin-app.js` trap in `gates/pending/README.md`, and worth knowing before trusting a hand-run green. |
| 31-antagonist373, #381 re-gate | the `Math.min(Math.max(...))` clamp dropped from the verdict label's `left`, so it is free to hang off the h-file again - the #373 defect #374 fixed | **FAILED, 2 of 14** | The label sits 6.1px past the board's right edge at 375x679 and 11.4px at 320x568, and only that assertion fires. Clean control for a gate whose whole subject is one antagonist finding. |
| 11-lesson, #381 re-gate | `_edge` forced false, so nothing is edge to edge | **FAILED, 2 of 16** | The demo board reads 368 wide at top 98 where the gate pins 375 at top 92. Worth noting WHY this one worked first time: 11-lesson PINS its numbers instead of comparing them to themselves, which is exactly what 10-gameover and A-12 did not do. |
| 21-review-brilliant, #381 re-gate | the play-out button removed (`_wasBest&&` -> `false&&`) | **FAILED, 2 of 7** | The button assertion goes red and then the harness throws on the locator it needs next, which is a fair failure - a gate that cannot proceed is not a gate that passed. |
| 30-p1-fixes A-12, #381 re-gate | the MOVES panel's space reservation removed (`visibility:hidden` -> `display:none`) | **FAILED 2 of 33 - BUT NOT ON THE ASSERTION THAT MATTERS** | Only the visibility assertion fired. The board did NOT move: measured 353/368/288/353 at 375x679, 390x844, 320x568 and 375x730, **identical on both bundles**. So the panel's reservation is not what holds the board steady on this screen, and A-12's comment describes an intention rather than the mechanism. **A-12's board claim therefore still has no control**, and saying so is the point: a gate that went red is not a gate that is proved. The width is now PINNED anyway (351/390), the same fix 10-gameover needed, which guards a board that is the wrong size throughout even though the across-toggle claim remains unfalsified. |
| 30-p1-fixes A-16, #381 re-gate, FIRST ATTEMPT | `marginBottom:40` on the menu sheet | **inert - sheet bottom 669 on both bundles** | Third non-control of the session, and this one reached the bundle (`marginBottom:40` is in it) and still moved nothing. The backdrop is `display:flex; align-items:flex-start` and the sheet is already clamped by `maxHeight:100%`, so a bottom margin extends past the item without reducing its height - and height is what A-16 measures. Checking that the break reaches the CODE is not enough; it has to reach the MECHANISM the assertion is about. |
| 30-p1-fixes A-16, #381 re-gate | the sheet's `maxHeight` from `100%` to `80%` - the shape of the pre-#373 defect, where 5vh + 88vh + padding left 36px of backdrop under it | **FAILED, 4 of 35** | Bottom 537 of 679 and 669 of 844, both the Home and the Discover measurement. |
| 30-p1-fixes A-10, #381 re-gate | the Next-puzzle onClick emptied | **FAILED, 4 of 35** | Both halves at both geometries: Next does not change the puzzle, and Previous therefore cannot return to the first. |
| 30-p1-fixes A-13, #381 re-gate | the #373 short-screen rhythm undone (`gap:vp.h<720?10:16` to a flat `gap:16`) | **FAILED, 1 of 35, at 375x640 ONLY** | Correct, and worth reading as a pass for the gate's DESIGN rather than a narrow control: the branch only exists below 720px, and the gate already carries a 375x640 sample for exactly that reason. At 375x679 and 390x844 nothing fires because nothing is wrong there. A gate that only ran his phone would have missed it. |
| 14-uat-review-card, #381 re-gate, FIRST ATTEMPT | the caption strip widened to 130vw | **inert** | The assertion is on the caption's BOTTOM, not its right edge. Fourth non-control: I broke a dimension the gate does not measure. |
| 14-uat-review-card, #381 re-gate | the caption strip's `bottom` moved to -60px | **FAILED, 1 of 14** | Caption bottom 739 against a 730 viewport, at the 52 s checkpoint. |
| 15-gallery-playall, #381 re-gate | reused the 10-gameover bundle: the MOVES header row's `minHeight:23` removed | **FAILED, 2 of 12 - AND IT REPRODUCED THE #378 DEFECT EXACTLY** | "no board shrinks inside a card" fires with board 375 -> 367 inside card k10, and "no card leaves the page scrolling" with 3px of overflow. That is precisely what Kunal saw at #378. The instructive part is the comparison: 10-gameover, the gate whose entire subject is card k10, was BLIND to this same bundle until its width was pinned last pass, while 15-gallery-playall caught it. The suite's redundancy earned its keep here. |

The lesson generalises past this gate: a suite that has never been run against a broken build is a suite whose
greens have not been earned. Where a behavioural assertion cannot reach the fault on this hardware, guard the
artifact directly and say which half is doing the work.
